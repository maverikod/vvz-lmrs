step_id: A-004
parent_tactical_step: T-001
name: Define RuntimeClient
target_file: lmrs/runtime_client.py
operation: modify_file
priority: 4
depends_on:
- A-003
concepts:
- C-015
- C-016
objects:
- RuntimeClient
prompt: Modify lmrs/runtime_client.py after A-001 through A-003. Append dataclass
  RuntimeClient with backend RuntimeBackend and an execute method or function that
  accepts an already-admitted request and a runtime profile, calls the selected backend,
  and returns NormalizedRuntimeResult on success or RuntimeFailureSignal on failure
  as contract stubs. It hides backend differences and must not make admission decisions
  or re-run admission. Do not generate public command metadata here.
verification:
  type: static_analysis
  target: lmrs/runtime_client.py::RuntimeClient
  expected: RuntimeClient executes admitted requests against a selected backend and
    returns a normalized result or a structured failure signal without making admission
    decisions.
status: done
