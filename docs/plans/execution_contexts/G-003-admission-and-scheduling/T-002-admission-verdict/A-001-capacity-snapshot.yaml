schema_version: lmrs.execution_context.v1
language: en
file_kind: as_context
context_bundle_order:
- ../../../00-common/common_context.yaml
- ../../../00-common/root_orchestrator_task.yaml
- ../../g_context.yaml
- ../t_context.yaml
- ./A-001-capacity-snapshot.yaml
role: atomic_step_executor
required_model: gpt-5.3-codex-spark
scope:
  global_step: G-003-admission-and-scheduling
  tactical_step: T-002-admission-verdict
  atomic_step: A-001
  source_file: docs/plans/G-003-admission-and-scheduling/T-002-admission-verdict/atomic_steps/A-001-capacity-snapshot.yaml
  target_file: lmrs/admission.py
atomic_step_spec:
  step_id: A-001
  parent_tactical_step: T-002
  name: Define CapacitySnapshot
  target_file: lmrs/admission.py
  operation: create_file
  priority: 1
  depends_on: []
  concepts:
  - C-012
  - C-047
  objects:
  - CapacitySnapshot
  prompt: Create lmrs/admission.py and define frozen dataclass CapacitySnapshot with usable_dynamic_vram_bytes, max_dynamic_pool_bytes, model_loaded bool, runtime_ready bool, and metadata. It is the current runtime capacity input for admission decisions. Do not add verdict or queue logic in this step.
  verification: &id001
    type: static_analysis
    target: lmrs/admission.py::CapacitySnapshot
    expected: CapacitySnapshot captures current and maximum dynamic VRAM plus model/runtime readiness.
  status: done
hrs_mrs_excerpts:
- concept_id: C-012
  name: Admission Control
  definition: The pre-queue decision process that classifies requests as executable, queueable, or rejected using context-window and VRAM capacity constraints.
  properties:
  - Admission runs before a request enters the queue.
  - Requests over the model context window are rejected with CONTEXT_OVERFLOW.
  - Requests too large for the maximum dynamic pool are rejected with REQUEST_TOO_LARGE or HARDWARE_VRAM_INSUFFICIENT.
  - Requests that fit the maximum dynamic pool but not current capacity are queued, not permanently rejected.
  - Requests fitting both model window and current dynamic pool may execute immediately or be queued according to scheduler policy.
  - LMCache reuse does not permit unsafe admission.
  source_labels:
  - '{adm1}'
  - '{adm2}'
  - '{adm3}'
  - '{inv1}'
  - '{inv2}'
  - '{lmcache2}'
  - '{lmcache6}'
  - '{inv5}'
- concept_id: C-047
  name: Request Dynamic VRAM Requirement
  definition: Per-request formula for dynamic VRAM required to execute a request safely at runtime.
  properties:
    formula: required_dynamic_vram_bytes = input_kv_bytes + reserved_output_kv_bytes + per_request_overhead_bytes + runtime_batch_overhead_bytes
    input_kv_bytes: Computed from the final tokenized prompt.
    reserved_output_kv_bytes: Computed from reserved output tokens.
    execution_predicate: required_tokens <= declared_context_window and required_dynamic_vram_bytes <= usable_dynamic_vram_bytes
  source_labels:
  - '{resv1}'
  - '{lim1}'
  - '{lim2}'
execution_rules:
  single_target_step: true
  assume_empty_initial_context: true
  use_only_concatenated_context: true
  no_guessing: true
  on_conflict_or_missing_fact: Escalate to the T orchestrator with exact file paths, command outputs, and the decision required.
acceptance:
- *id001
- Report changed files and verification evidence to the T orchestrator.
