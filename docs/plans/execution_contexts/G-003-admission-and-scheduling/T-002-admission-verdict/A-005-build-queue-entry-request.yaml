schema_version: lmrs.execution_context.v1
language: en
file_kind: as_context
context_bundle_order:
- ../../../00-common/common_context.yaml
- ../../../00-common/root_orchestrator_task.yaml
- ../../g_context.yaml
- ../t_context.yaml
- ./A-005-build-queue-entry-request.yaml
role: atomic_step_executor
required_model: gpt-5.3-codex-spark
scope:
  global_step: G-003-admission-and-scheduling
  tactical_step: T-002-admission-verdict
  atomic_step: A-005
  source_file: docs/plans/G-003-admission-and-scheduling/T-002-admission-verdict/atomic_steps/A-005-build-queue-entry-request.yaml
  target_file: lmrs/admission.py
atomic_step_spec:
  step_id: A-005
  parent_tactical_step: T-002
  name: Implement build_queue_entry_request
  target_file: lmrs/admission.py
  operation: modify_file
  priority: 5
  depends_on:
  - A-004
  concepts:
  - C-013
  - C-012
  objects:
  - build_queue_entry_request
  prompt: 'Modify lmrs/admission.py after A-001 through A-004. Append build_queue_entry_request(verdict: AdmissionVerdict, estimate) -> dict[str, object]. It must only accept queueable verdicts, include request_id, model_name, required_tokens, required_dynamic_vram_bytes, admission decision timestamp placeholder, and reason_code. Do not implement queue storage or scheduling here.'
  verification: &id001
    type: static_analysis
    target: lmrs/admission.py::build_queue_entry_request
    expected: Function converts a queueable verdict and estimate into a structured queue input and rejects non-queueable verdicts.
  status: done
hrs_mrs_excerpts:
- concept_id: C-012
  name: Admission Control
  definition: The pre-queue decision process that classifies requests as executable, queueable, or rejected using context-window and VRAM capacity constraints.
  properties:
  - Admission runs before a request enters the queue.
  - Requests over the model context window are rejected with CONTEXT_OVERFLOW.
  - Requests too large for the maximum dynamic pool are rejected with REQUEST_TOO_LARGE or HARDWARE_VRAM_INSUFFICIENT.
  - Requests that fit the maximum dynamic pool but not current capacity are queued, not permanently rejected.
  - Requests fitting both model window and current dynamic pool may execute immediately or be queued according to scheduler policy.
  - LMCache reuse does not permit unsafe admission.
  source_labels:
  - '{adm1}'
  - '{adm2}'
  - '{adm3}'
  - '{inv1}'
  - '{inv2}'
  - '{lmcache2}'
  - '{lmcache6}'
  - '{inv5}'
- concept_id: C-013
  name: Request Queue
  definition: The queue of admitted requests waiting for dynamic VRAM capacity or scheduler selection.
  properties:
  - Queued records store request_id, session_id, model_name, token counts, required dynamic VRAM, admission timestamp, expiration timestamp, priority, and status.
  - Queued requests must pass capacity recheck before actual launch.
  source_labels:
  - '{que1}'
  - '{rech}'
execution_rules:
  single_target_step: true
  assume_empty_initial_context: true
  use_only_concatenated_context: true
  no_guessing: true
  on_conflict_or_missing_fact: Escalate to the T orchestrator with exact file paths, command outputs, and the decision required.
acceptance:
- *id001
- Report changed files and verification evidence to the T orchestrator.
