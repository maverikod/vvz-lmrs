step_id: A-002
parent_tactical_step: T-003
name: Define RequestQueue
target_file: lmrs/queue.py
operation: modify_file
priority: 2
depends_on:
- A-001
concepts:
- C-013
objects:
- RequestQueue
prompt: Modify lmrs/queue.py after A-001. Append dataclass RequestQueue with 
  entries tuple[QueueEntry, ...] default empty and methods or functions to add, 
  cancel, and snapshot entries without scheduling. It must keep immutable-style 
  returned queue state and not execute runtime calls.
verification:
  type: static_analysis
  target: lmrs/queue.py::RequestQueue
  expected: RequestQueue owns queued entries and supports queue state 
    mutation/snapshot semantics without scheduling or runtime execution.
status: done
