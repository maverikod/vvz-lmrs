"""Verification pipeline package: the single verification entrypoint for LMRS.

The package ships the ``pipeline`` console script (see ``pipeline.cli``); every
test and every check of the project is registered here as a named check.
"""
